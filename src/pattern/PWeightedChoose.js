/**
 * @depends Pattern.js
 */

var PWeightedChoose = new Class({
    Extends: Pattern,
    initialize: function() {
        Pattern.prototype.initialize(this);
    },

    next: function() {
    }
});

Pwrand = PWeightedChoose;
